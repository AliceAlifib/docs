---
title: "Connect your agent"
description: "Wire the coding agent you already use into your project record."
---

Alice.In does not run an AI agent for you. It connects to the agent you already use, on your machine and your own subscription. [Claude Code](https://claude.com/claude-code) is the fullest experience; Codex and Grok reach the same workspace.

## Before you start

Have your agent installed and signed in first — [Claude Code](https://claude.com/claude-code), Codex or Grok Build, on your own account. Alice.In connects to it; it does not install it for you.

You also need **Node 18 or newer**, because the capture hooks are Node scripts. The installer checks and stops with a clear message if it is missing.

## The first time

Sign in from the invite at [alicein.app/start](https://alicein.app/start). Google, then the Connect page. That page already provisioned your workspace; the command on it already includes your token.

Pick **macOS / Linux** or **Windows** on that page, press **Copy command**, and paste it into a terminal *inside the repo you work in*. You do not need to copy the token separately — it is already in the line. Reveal it on the page only if you are wiring something by hand.

The first-run command wires Claude Code and Codex together (Grok rides the same files). Unused config just sits there.

When it finishes, it prints a short summary: which agents it connected, that your token is kept private, and what to do next — start Claude Code in that folder and type `/alice-onboard`. If it also wired Codex, it adds one first-time step for Codex, covered [below](#if-you-use-codex-or-grok).

## What that command actually does

It does not install an AI. It does not run a daemon. It writes a handful of files into **that repo**:

1. Points your agent at Alice.In over the network (the “door”), with a token that lives in a gitignored config file — not in the shared `.mcp.json`.
2. Drops in a short brief and a few small hooks — on commits, prompts, file reads and the end of each turn — so work lands in the project record as you go.
3. **Adds** one line to your existing `CLAUDE.md`. For Codex and Grok it **prepends** a managed block to `AGENTS.md` — their files are read from the top, so a line at the bottom would never load. Your own instructions stay.
4. Adds `/alice-onboard` for Claude Code. Codex and Grok say “onboard this project” instead.

Re-running is safe. It never replaces your house rules.

## Installing another repo later

Once you can see the project map, open the **⚙** at the bottom right, beside the agent-queue button, and press **Open** on *Connect a repo*.

The panel shows your door URL, the repos already connected, and a row of agent buttons — **Both**, **Claude Code**, **Codex** or **Grok Build**. Press **Generate install command**, and paste the one-liner into a terminal inside that repo.

<CodeGroup>
```bash macOS, Linux, Git Bash
curl -fsSL https://<your-hub>/install.sh | sh -s -- --token <your-token> --url https://<your-hub>/mcp
```

```powershell Windows PowerShell
& ([scriptblock]::Create((irm https://<your-hub>/install.ps1))) --token <your-token> --url https://<your-hub>/mcp
```
</CodeGroup>

<Note>
`curl … | sh` does not run in PowerShell, which is why there are two lines. Everything underneath is the same plain Node script either way.
</Note>

**Which agent button to pick.** *Both* is the default and wires Claude Code and Codex together — if you have one of them, the unused config just sits there doing nothing. The named buttons exist so the line you copy talks about the agent you actually use.

**Grok Build users should pick Grok Build**, not Claude Code. Grok reads Claude Code's wiring natively, with one exception: it cannot follow the `@import` line in `CLAUDE.md`, so picking *Claude Code* leaves you with the connection and the hooks but no brief at all. The Grok option inlines it instead.

Add `--dir <path>` to target a different folder. Re-running is safe — it updates in place and never duplicates anything.

You can connect as many repositories as you like. **Connect a repo** lists what is already connected. They share the same workspace token — it is one project, several checkouts.

## What gets installed

Plain files, all of them readable, editable and deletable:

| | |
|---|---|
| `.mcp.json` | Registers the connection. Any MCP servers you already had are kept. |
| `.claude/alice/` | Your agent's brief, the capture hooks, and your config (token stays gitignored). |
| `CLAUDE.md` | One extra line that loads the brief. Your existing file is not replaced. |
| `AGENTS.md` | A managed block at the top for Codex and Grok. Your rules stay below it. |
| `.claude/settings.json` | Small hooks that keep your workspace in sync as you work. |
| `.claude/skills/alice-onboard` | The `/alice-onboard` command. Codex and Grok say “onboard this project”. |

Nothing runs in the background, and nothing about your existing setup changes.

<Note>
**Your token never goes into `.mcp.json`.** That file is meant to be committed and shared with your team, so it gets a small helper command that reads the token from `.claude/alice/config.json` instead — and the installer adds that file to your `.gitignore`. A teammate who clones the repo inherits the wiring and supplies their own token.

This needs Claude Code 2.1.193 or newer. On an older build the installer can fall back to writing the token into `.mcp.json` directly, in which case keep the repo private.
</Note>

## If you use Codex or Grok

<Warning>
**Codex asks you to trust twice** the first time you open it in the repo — once for the folder, once for the hooks file. Accept both. An untrusted folder loads no tools at all, and nothing tells you why.
</Warning>

**Codex also needs your token in its environment.** Claude Code reads it from the private config file; Codex can only take it from a variable. You still do not copy it — the install saved it in `.codex/alice/config.json`, and the line it prints reads it from there. Run it in the terminal you start Codex from, inside the repo:

<CodeGroup>
```powershell Windows PowerShell
$env:ALICEIN_DOOR_TOKEN = (Get-Content .codex/alice/config.json | ConvertFrom-Json).token
```

```bash macOS, Linux, Git Bash
export ALICEIN_DOOR_TOKEN="$(node -p 'require("./.codex/alice/config.json").token')"
```
</CodeGroup>

It lasts as long as that terminal. In a new one, run it again before starting Codex.

**Grok Build** reads Claude Code's files, so the install puts the same wiring in place plus a brief Grok can load. Trust the folder the first time you open it, then run `grok inspect` to see the door, the brief and the hooks it found.

Neither has a per-repo skills folder, so they reach [onboarding](/using/onboarding) through a line in `AGENTS.md` — say "onboard this project" rather than typing a slash command.

## Check it worked

- Run `/mcp` in Claude Code. **alicein** should be listed. In Grok, `grok inspect`.
- Ask **"where are we?"**. It should answer from your real project — your phases, recent activity, anything queued for it — not from a blank slate.

If your workspace is still empty, this is the moment to run [`/alice-onboard`](/using/onboarding).

## What changes in your sessions

<CardGroup cols={2}>
  <Card title="It arrives oriented" icon="compass">
    Your agent starts knowing the whole project — phases, active work, recent decisions, what is waiting on you.
  </Card>
  <Card title="It leaves a record" icon="pen">
    Work normally. It captures what it did and why as it goes, so nothing you build stays unexplained.
  </Card>
  <Card title="Files brief it as it opens them" icon="file-lines">
    The moment it reads or edits a file, whatever your project already learned about that file is handed to it. If nothing is recorded, it says so plainly — so "nothing known" never looks like "not working".
  </Card>
  <Card title="It picks up queued work" icon="inbox-in">
    Press **Queue for agent** on a card, then say **"check my alicein queue"**. The card arrives with its full context pack.
  </Card>
  <Card title="Commits light the map" icon="code-commit">
    Every commit is captured and matched to the part of your system it touched. You add the why.
  </Card>
  <Card title="It proposes, you decide" icon="check">
    Changes to plans and decisions land in [Needs you](/using/needs-you) for you to ratify. Nothing structural changes without you.
  </Card>
</CardGroup>

For the full picture of what it can and cannot do, see [What your agent can do](/concepts/what-your-agent-can-do).

## Using a different tool

Any card offers its context as markdown you can copy into Cursor, ChatGPT, or whatever you prefer. You lose the automatic capture, not the context.

## Uninstalling

A minute by hand: delete `.claude/alice/` and `.claude/skills/alice-onboard/`, remove the `@.claude/alice/ALICE-COFOUNDER.md` line from `CLAUDE.md`, remove the `alicein` entry from `.mcp.json`, and remove the entries that point into `.claude/alice/` from `.claude/settings.json`. If Codex was wired too, delete `.codex/alice/`, remove the Alice.In entries from `.codex/hooks.json`, and delete the marked Alice.In blocks in `.codex/config.toml` and at the top of `AGENTS.md`. Then restart your agent.

<Note>
We never see or spend your Claude account, and we never ask for its credentials.
</Note>
